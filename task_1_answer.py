import re


def solve():

    with open('data.txt', 'r', encoding='utf-8') as f:
        s = f.read().strip()

    # Регулярное выражение 'Q{2,}' находит все вхождения QQ, QQQ, QQQQ и т.д.
    # Мы разбиваем строку по этим разделителям
    parts = re.split(r'Q{2,}', s)
    print(len(max(parts, key=len)))
#     # Если разделителей меньше двух, то подходящих подстрок нет
#     if len(parts) < 3:
#         # Нужна хотя бы одна подстрока МЕЖДУ двумя группами QQ
#         # Это значит после split должно быть минимум 3 элемента
#         # Пример: QQ ABC QQ -> split даст ['', 'ABC', '']
#         print(0)
#         return
#
#     max_len = 0
#     # Нас интересуют только те части, которые стояли МЕЖДУ разделителями.
#     # В результате split это все элементы, кроме первого и последнего,
#     # если QQ стоят по краям. Но на самом деле, любая часть,
#     # полученная после split (кроме крайних, если там не было QQ), подходит.
#
#     # Правило: подстрока должна быть ОГРАНИЧЕНА с двух сторон.
#     # Значит, мы берем все элементы массива parts, которые находятся
#     # МЕЖДУ первым и последним найденным разделителем.
#
#     # Если строка 'ABCQQDEFQQGHI', parts будет ['ABC', 'DEF', 'GHI']
#     # Подстрока 'DEF' ограничена QQ с двух сторон.
#
#     # Проходим по всем частям, кроме первой и последней:
#     for i in range(1, len(parts) - 1):
#         max_len = max(max_len, len(parts[i]))
#
#     print(max_len)
#
#
# # Запуск
solve()